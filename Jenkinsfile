pipeline {
    stages{
        stage('git'){
            steps {

            }
        }
        stage('maven'){
            steps{
                sh 'mvn clean'
                sh ,mvn build'
            }

        }
    }
}
